package org.firstinspires.ftc.teamcode;

/// Describes which of the robot's drive motors are used.
public enum Traction {
    /// Use only the back drive motors.
    Back,
    /// Use only the front drive motors;
    Front,
    /// Use all motors (default).
    Both
}
